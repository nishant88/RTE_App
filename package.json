{
  "name": "rte-study-planner",
  "version": "1.0.0",
  "description": "RTE study planner with cron scraper and progress tracker",
  "scripts": {
    "install-deps": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
